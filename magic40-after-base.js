/* magic40 production: a brand-new student must start clean. */
if (!magic40HadPersonal) {
  state.tasks={}; state.sessions=[]; state.exams=[]; state.calendarEvents=[]; state.magicMemory=[];
  state.reviewCompletions={}; state.cancelledReviews={}; state.reviewTimeRecords={};
}